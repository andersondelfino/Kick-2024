const prompt = require("prompt-sync")();
let numeros = prompt('digite seu numero para ser multiplicado');
let numero = parseInt(numeros);
 
for (let cont = 0; cont <= 10; cont++) {
    const result = numero * cond;
    console.log(result);
   
}